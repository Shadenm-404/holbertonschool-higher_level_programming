# RESTful API – Task 02

This task demonstrates how to consume a public REST API using Python.

## Description
The script uses the Python requests library to fetch posts from the JSONPlaceholder API.

## Features
- Sends an HTTP GET request to a public API
- Prints the HTTP status code
- Parses JSON data and prints post titles
- Saves selected post data (id, title, body) into a CSV file
